# Required by /api/scan-menu.js — the Claude Vision menu scanner.
# Get a key at https://console.anthropic.com/settings/keys and set it in your Vercel project's
# Environment Variables (or in a local .env for `vercel dev`). Never commit the real value.
ANTHROPIC_API_KEY=
